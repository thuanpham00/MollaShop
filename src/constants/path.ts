export const path = {
  home: "/", // trang chủ
  productList: "/productList",
  profile: "/profile",
  login: "/login",
  register: "/register",
  logout: "/logout"
} as const

// chỉ có home và productList không cần đăng nhập
